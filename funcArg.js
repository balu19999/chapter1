//arguments and parameters
// let test=function(name,age)
// {
//   console.log(`the name is ${name} and the age is ${age}`);
// }
// //console.log(`the name is ${name} and the age is ${age}`); we cant use like this bcoz the arguments are local variables

// test('balu',23);
// test('ajay',24);
// test('pruthvi',23);
// test();//it shows undefined

// let test=function(name='balu',age=24)
// {
//   console.log(`the name is ${name} and the age is ${age}`);
// }
// //console.log(`the name is ${name} and the age is ${age}`); we cant use like this bcoz the arguments are local variables

// // test('balu',23);
// // test('ajay',24);
// // test('pruthvi',23);
// test();//it shows the already intialized values
// test('shetty',23); //it overrides the intialized values
// test('ajay');//if we not overrideone of intialized value it defaltly take the intial values

